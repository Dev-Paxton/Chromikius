export interface DatabaseSettings {
    host: string
    databaseName: string
    userName: string
    password: string
}

export interface BotSettings {
    id: string
    token: string 
}

export interface GuildSettings {
    id: string
}